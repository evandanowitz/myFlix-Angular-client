/**
 * Bootstraps the app for server-side rendering.
 * 
 * @remarks
 * This file serves as the entry point for the server-side version of the app.
 * It exports the `AppServerModule` by default, which is used for SSR.
 */
export { AppServerModule as default } from './app/app.module.server';
